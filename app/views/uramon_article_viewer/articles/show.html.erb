<%= render partial: 'breadcrumbs', locals: {breadcrumbs: [['ホーム', '/'], [@article.category_name, category_path(@article.category_slug)], [@article.title]]} %>

<article>
  <%= raw render_markdown(@article.body) %>
</article>

<%= link_to 'Back', root_path %>
