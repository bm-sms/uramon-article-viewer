<%= render partial: 'breadcrumbs', locals: {breadcrumbs: [['ホーム', '/'], [@category.name]]} %>

<h1>Categories#show</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article.id) %>
    </li>
  <% end %>
</ul>
